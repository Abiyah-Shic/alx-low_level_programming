#include "main.h"
/**
 * print_last_digit - A function that prints the last digit of a number.
 * @n: The number that we are printing its last digit
 * Return: last_digit
 */
int print_last_digit(int n)
{
int last_digit = (n % 10);
return (last_digit);
}
